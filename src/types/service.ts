export interface ServiceOverviewItem {
  title: string;
  description: string;
  icon: string;
  href: string;
  badge?: "CORE" | "NEW";
}

export interface AeoGeoFeature {
  title: string;
  description: string;
  href: string;
}
